{
  "name": "nodejs-enterprise-demo",
  "private": true,
  "version": "1.0.0",
  "description": "Production-ready Node.js + TypeScript monorepo with enterprise features for MNCs",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lint": "eslint . --ext .ts,.js",
    "format": "prettier --write .",
    "test": "jest",
    "dev": "ts-node-dev --respawn packages/api-gateway/src/index.ts"
  },
  "devDependencies": {
    "dotenv": "^16.0.0",
    "eslint": "^8.0.0",
    "husky": "^9.0.0",
    "jest": "^29.0.0",
    "prettier": "^3.0.0",
    "supertest": "^6.0.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@prisma/client": "^6.14.0",
    "prisma": "^6.14.0"
  }
}
